var express = require('express');
var app = express();
var port = 4000;
app.listen(port, function () {
    console.log("Success! Your application is running on port ".concat(port, "."));
});
//# sourceMappingURL=server.js.map